This is directory demonstrates the method of creating static libraries in C

1. A static library named libmy.a is created.

2. A shell script creates_static_lib.sh is written to create stattic libraries.
